import { StyleSheet } from "react-native";
import * as util from "vplan-util";

export default StyleSheet.create({
  container: {
    paddingLeft: 14,
    paddingVertical: 14,
    backgroundColor: util.blueGray
  },
  text: {
    color: util.white,
    fontWeight: "bold"
  }
});
