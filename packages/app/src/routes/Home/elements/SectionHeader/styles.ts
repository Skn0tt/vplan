import { StyleSheet } from "react-native";

const radius = 44;

export default StyleSheet.create({
  container: {
    paddingLeft: 14,
    paddingVertical: 14,
    backgroundColor: "#FFFFFFDC"
  },
  text: {
    color: "#616161",
    fontWeight: "bold"
  }
});
