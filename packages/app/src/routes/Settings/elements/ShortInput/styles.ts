import { StyleSheet } from "react-native";
import * as util from "vplan-util";

export default StyleSheet.create({
  container: {},
  text: {
    fontWeight: "bold",
    fontSize: 20
  },
  input: {
    width: "100%",
    fontSize: 16
  }
});
